# --- Site ---
PUBLIC_SITE_URL="https://webinar100k.example.com"

# --- Analytics (optional; if empty, scripts are not injected) ---
# Google Analytics 4 measurement ID, e.g. G-XXXXXXXXXX
PUBLIC_GA_MEASUREMENT_ID=""
# Facebook Pixel ID, e.g. 1234567890123456
PUBLIC_FB_PIXEL_ID=""

# --- WayForPay ---
# Public values (safe to expose to the browser)
PUBLIC_WAYFORPAY_MERCHANT_ACCOUNT="test_merch_n1"
PUBLIC_WAYFORPAY_MERCHANT_DOMAIN="webinar100k.example.com"

# Server-only secret (NEVER expose to the browser).
# Used to compute HMAC_MD5 signature inside /api/wayforpay/signature.
WAYFORPAY_MERCHANT_SECRET_KEY="flk3409refn54t54t*FNJRET"
