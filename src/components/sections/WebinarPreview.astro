<section class="section webinar-preview">
  <div class="container webinar-preview__inner">
    <div class="section-head">
      <span class="eyebrow">Фрагмент вебінару</span>
      <h2>Подивись частину вебінару перед доступом</h2>
      <p>Короткий приклад того, як я пояснюю систему монетизації Instagram простими кроками.</p>
    </div>

    <div class="webinar-preview__video">
      <video src="/reviews/preview_webinar.mp4" controls playsinline preload="metadata"></video>
    </div>

    <div class="webinar-preview__cta">
      <a href="#pricing" class="btn btn-primary">Дивитися весь вебінар</a>
    </div>
  </div>
</section>

<style>
  .webinar-preview {
    padding-top: clamp(28px, 5vw, 64px);
  }

  .webinar-preview__inner {
    max-width: 920px;
  }

  .webinar-preview__video {
    overflow: hidden;
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border);
    background: var(--color-bg-elev);
    box-shadow: var(--shadow-card);
  }

  .webinar-preview__video video {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    background: #000;
  }

  .webinar-preview__cta {
    display: flex;
    justify-content: center;
    margin-top: clamp(22px, 4vw, 34px);
  }
</style>
